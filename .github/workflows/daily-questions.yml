name: Daily Questions

on:
  schedule:
    # Run at ~00:01 US Eastern (EDT/EST covered by two crons)
    - cron: '1 4 * * *'   # ~00:01 EDT (summer)
    - cron: '1 5 * * *'   # ~00:01 EST (winter)
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily5
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate today's questions (ET-aware)
        run: node scripts/generate-daily.cjs
        shell: bash

      - name: Commit & push if changed (daily.json + used.json)
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "daily5-bot"
          git config user.email "daily5-bot@users.noreply.github.com"

          # Only commit if either file changed
          if ! git diff --quiet --exit-code -- daily.json used.json; then
            git add daily.json used.json
            git commit -m "chore: update daily.json (+ used.json)"
            git push
          else
            echo "No changes to commit."
          fi
